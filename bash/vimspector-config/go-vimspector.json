{
  "configurations": {
    "run": {
      "adapter": "vscode-go",
      "configuration": {
        "request": "launch",
        "program": "${workspaceRoot}/${go_file}",
        "mode": "debug",
        "dlvToolPath": "${GOPATH}/bin/dlv"
      }
    }
  }
}
