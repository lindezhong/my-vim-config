{
  "configurations": {
    "run": {
      "adapter": "vscode-go",
      "configuration": {
        "name": "go调试程序: 当前文件",
        "request": "launch",
        "program": "${workspaceRoot}/${file}",
        "args": [],
        "mode": "debug",
        "dlvToolPath": "${GOPATH}/bin/dlv"
      }
    }
  }
}
