# 在文件上的每行行首添加行号
# `sed '=' table.md` 将文件变为, `行号 \n 内容`  格式
# `sed 'N;s/\n/: /'` 将其合并为一行 
sed '=' table.md | sed 'N;s/\n/: /'

# 去除重复行, 其中 -z 表示将整个文件作为'一行'输入 
sed -z 's/\n\(.*\)\n\1//g' table.md

# 只显示匹配项目, -n 禁止自动输出，仅输出匹配的内容 (需要配合/p使用) 
sed -n 's/form/to/p' table.md
